{
	"name": "remoto-console",
	"version": "1.0.0",
	"description": "Remoto online console application for Google App Engine.",
	"main": "index.js",
	"scripts": {
		"test": "go test ./...",
		"build-css": "node-sass src/styles.scss --output-style compressed --source-map true -o static",
		"watch-css": "node-sass src/styles.scss -w --output-style compressed --source-map true -o static",
		"build-js": "webpack"
	},
	"author": "Machine Box, Inc.",
	"license": "ISC",
	"dependencies": {
		"ace-builds": "^1.3.3",
		"bootstrap": "^4.1.1"
	},
	"devDependencies": {
		"node-sass": "^4.9.0",
		"webpack": "^4.12.0",
		"webpack-cli": "^3.0.3"
	}
}
